{
  "name": "lumen-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Lumen Academy platform: the multi-tenant API (server/) and the staff, parent and teacher web apps (web/).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "dev:api": "npm run dev --workspace server",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "build:api": "npm run build --workspace server",
    "build:web": "npm run build --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run lint --workspace web",
    "lint": "npm run lint --workspace server",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "test:api": "npm run test --workspace server",
    "test:web": "npm run test --workspace web",
    "migrate": "npm run migrate --workspace server",
    "seed": "npm run seed --workspace server"
  }
}
